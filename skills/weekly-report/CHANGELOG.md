# weekly-report — 변경 이력

| 버전 | 날짜 | 변경 |
|------|------|------|
| v2.2.0 | 2026-09-01 | **드라이브 폴더 `parent/author` 자동 조립** — 설정에 부모 폴더만 적고 본인 이름 폴더는 `publish.ps1` 이 만든다(daily-report 와 공유). 처음 연결하는 경우 `daily-report` 스킬 §0.1 7단계를 따르도록 포인터 추가 |
| v2.1.0 | 2026-09-01 | **첫 실행 설정 프롬프트** — 설정이 `report-config.json` 으로 옮겨졌고 daily-report 스킬과 **같은 파일을 공유**한다(한쪽에서 만들었으면 다시 묻지 않는다). 파일이 없거나 `author` 가 비면 이름·직함을 한 번 묻고 만든다. 프레임 주석의 `config.ps1` 참조도 정정 |
| v2.0.0 | 2026-09-01 | **룰(`.claude/rules/weekly-report-template.md`) → 스킬로 전환 + 독립화.** ①frontmatter 부여 — 상시 로드에서 호출 시 로드로 ②CHANGELOG 분리 ③프레임에 하드코딩돼 있던 작성자(`박성찬 연구원`)를 `{{AUTHOR}}` 플레이스홀더로, 값은 `config.ps1` 참조 ④타임라인 링크 출처를 개인 메모리(`[[reference-daily-report-artifacts]]`)에서 저장소 파일(`artifact-urls.md`)로 이관 + 인덱스가 비어도 정상 동작함을 명시 ⑤**§13 절차에 발행·업로드 단계 신설** — 실제로는 `publish.ps1` 로 올리고 있었는데 문서에 없었다(주간 PDF 가 드라이브에 있는데 절차만 누락) ⑥실재하지 않는 "settings.json PostToolUse 리마인더 hook" 언급 삭제 ⑦회의 요일 고정(수요일) 서술을 일반화. **프레임 CSS·레이아웃 무변경**(v1.6.1 그대로) |
| v1.6.1 | 2026-06-30 | eyebrow 에서 '5분 발표' 라벨 제거(`주간 정기회의 · {{PROJECT}}`). 구조 변경 없음 |
| v1.6.0 | 2026-06-30 | **다음 단계 옵트인 + 일일 링크 자동화** — ①'다음 단계 · 논의'는 기본 미표시(사용자 지시 시에만; 타임라인이 마지막 섹션, per-card '다음'이 다음 액션 담당) ②타임라인 `↗ 일일 보고서` 링크를 인덱스에서 자동 조회·연결 |
| v1.5.0 | 2026-06-30 | **5분 발표용으로 최적화** — 과제마다 **상태 배지(완료/진행중/예정) + 진행률 바**, 카드 본문 `핵심 작업→성과` → **`현황→다음`**, 히어로 대시보드 `커밋/PR` → **`완료/진행 중`**, 일자별 타임라인을 선택으로 강등. 항목당 ~1분 가이드 |
| v1.4.0 | 2026-06-30 | **메인 과제 3~4개 허용** — 기본 3, 도메인 중요 작업이 별도 축이면 4번째 카드(`w4`). 타임라인·다음단계 번호 동적(`{{NT}}`·`{{NN}}` = K+1·K+2) |
| v1.3.0 | 2026-06-30 | **고대비 팔레트로 재정의** — 일일의 저채도 톤이 애매하다는 피드백 반영. 선명한 블루 accent(#0a5fc2)·진한 ink/line·또렷한 accent-soft·카드 그림자로 대비 강화. 히어로 그라데이션도 더 선명하게 |
| v1.2.1 | 2026-06-30 | 타임라인 표시 규칙 — **토·일은 일일 보고서 있을 때만 행 표시**(없으면 생략), 평일은 빈 날도 `작업 없음` 표시. 주말 잡음 제거 |
| v1.2.0 | 2026-06-30 | **일자별 타임라인 복구** — 메인 과제 카드 다음에 세로 레일 타임라인 복원(보고 기간 7행). 활동 막대 차트는 계속 두지 않음 |
| v1.1.0 | 2026-06-30 | **정기회의 보고서로 재편** — ①보고 주기·기간 규칙 ②제목 규칙 ③메인 과제 = 큰 카테고리 3개 고정 ④카테고리별 **데모뷰**(캡처/GIF data-URI · 인라인 SVG 스키매틱 · placeholder). v1.0.0 의 활동 차트는 '핵심만' 원칙으로 제거 |
| v1.0.0 | 2026-06-30 | 초기 — 일일 v1.3.0 토큰·컴포넌트 상속 + 주간 시각 레이어(그라데이션 히어로·주차 고스트 숫자·스탯 대시보드·요일별 활동 차트·TAG 분포 바·스크롤 등장/호버 모션·세로 레일 타임라인) |
