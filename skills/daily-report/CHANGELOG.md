# daily-report — 변경 이력

| 버전 | 날짜 | 변경 |
|------|------|------|
| v2.2.1 | 2026-09-14 | **목차가 한 칸 아래를 선택해 보이던 것 수정** — 사용자 신고 *"2번을 누르면 2번 챕터로 이동하긴 하는데 정작 3번을 보고 있는 듯이 목차에 표시됨"*. 원인 둘: ①감지 밴드가 `-18% 0px -72% 0px`(뷰포트 18~28%)인데 스크롤 앵커는 `scroll-margin-top: 70px` 이라 **창이 높을수록 간격이 벌어져** 짧은 절이 통째로 밴드 위로 빠지고 다음 절이 잡혔다 ②여럿 걸리면 `entries.forEach` 의 **마지막 entry** 가 이겨 문서 순서가 아니라 콜백 전달 순서로 정해졌다. ⇒ `IntersectionObserver` 를 걷어내고 **위치를 직접 계산**한다(앵커선 80px 보다 위에서 시작한 마지막 절 · rAF 스로틀 스크롤 리스너 · 절이 열 개 남짓이라 비용 무시). 아울러 **마지막 항목이 어떤 창에서도 켜지지 않던 것**(더 스크롤할 수 없어 밴드에 못 들어감)을 문서 끝 판정으로 살렸고, 클릭 즉시 활성화해 부드러운 스크롤 중에도 누른 항목이 켜져 있다. ★실브라우저 실측(6창 × 8항목 × **2축**〔목차 클릭 · 직접 스크롤〕 = 96회): **76/96 → 96/96**. 종전 어긋남 20건은 **창 높이에 비례**했다 — 1400×800 **0건** vs 1600×2000 **10건**. ⚠️ 클릭 축만 재면 「클릭 고정」만으로도 통과해 **공허**하다 — 직접 스크롤 축을 함께 재야 위치 계산이 실제로 도는지 갈린다 |
| v2.2.0 | 2026-09-01 | **드라이브 폴더를 `parent/author` 로 자동 조립 + 연결 안내를 스킬이 수행.** ①설정에서 `rclone.folder`(전체 경로) 를 없애고 **`rclone.parent`(공유 부모 폴더)만** 받는다 — 실제 경로는 `parent/author` 이고 `publish.ps1` 이 `rclone mkdir` 로 만든다. 여러 사람이 같은 부모를 공유해도 각자 이름 폴더라 안 겹친다 ②`author` 에 경로 문자(`/ \ : * ? " < > \|`)가 있으면 거부 ③**§0.1 드라이브 연결 7단계 신설** — 설치 확인 → `rclone config` 입력 순서 제시 → `lsd` 로 연결 검증 → 최상위 목록 보여주고 부모 폴더 선택 → 본인 폴더 생성까지 **내가 이끈다**. ⚠️ `rclone config` 는 대화형 TUI 라 **내가 실행하면 멈춘다** — 사용자가 치게 하고 나는 앞뒤를 검증한다 |
| v2.1.0 | 2026-09-01 | **첫 실행 설정 프롬프트** — 설정을 `config.ps1`(PowerShell·UTF-8 BOM 필수) 에서 `report-config.json` 으로 옮기고, 스킬이 **파일이 없거나 `author` 가 비면 이름·직함을 한 번 묻고 만든다**. 다음부터는 읽기만 한다. `publish.ps1` 은 그 JSON 을 `Get-Content -Encoding UTF8 \| ConvertFrom-Json` 으로 읽으며, **`rclone.folder` 가 비면 PDF 만 만들고 업로드를 건너뛴다**(rclone 없는 사람도 PDF 는 얻는다). 사유: 받는 사람이 `.ps1` 을 BOM 유지하며 손편집해야 했다 — 배포 마찰의 대부분이 그 한 단계였다 |
| v2.0.0 | 2026-09-01 | **룰(`.claude/rules/daily-report-template.md`) → 스킬로 전환 + 독립화.** ①frontmatter(`name`/`description`) 부여 — 매 세션 40 KB 상시 로드에서 호출 시 로드로 ②CHANGELOG 를 이 파일로 분리 ③작성자 하드코딩(`박성찬`) 제거 → `config.ps1` 의 `$AUTHOR`·`$AUTHOR_TITLE` 참조, 없으면 1회 질문 ④주간 URL 인덱스 의존을 개인 메모리(`[[reference-daily-report-artifacts]]`)에서 저장소 파일(`artifact-urls.md`)로 이관 ⑤프로젝트 고유 예시(패키지명·빌드 명령)를 일반화 ⑥발행 스크립트 부재 시 업로드 건너뛰고 보고하는 degrade 경로 명시. **프레임 구조·CSS 무변경**(v1.14.0 그대로 — 버전 주석·footer 만 동기화) |
| v1.14.0 | 2026-08-31 | **그림자 3단계 토큰 신설(`--shadow-sm`/`md`/`lg`)** — 개선 결과 카드를 가장 강하게, 작업·영향·검증 카드는 중간, 핵심 정리는 약하게, 칩·배지는 아주 약하게. 목차·핵심정리 참조칩은 hover 시 한 단계 상승. 4개 테마 블록 모두 정의, 인쇄에서는 제거. 사유: 기존 그림자가 매우 약해 입체감·가시성이 부족했다 |
| v1.13.0 | 2026-08-31 | **개선 결과 전용 색(`--perf-*`)** — 본문 저채도 팔레트를 그대로 쓰면 지표가 흐려진다. 여기만 채도를 올려 **이전=적색 / 개선=청록** 두 축으로 못박고 **초록 배지를 폐기**(개선값 청록과 구분이 안 됐고 색이 셋이면 위계가 흐려진다). 배지는 청록 채움 + 흰 글씨, 카드 좌측 청록 띠, 미니바 적색/청록 대비. 4개 테마 블록 모두 정의 |
| v1.12.1 | 2026-08-31 | 지표 숫자 전용 스택 `--num` 신설 — 등폭 대신 **각진 그로테스크 산세리프**로. 자릿수 정렬은 `tabular-nums` 로 유지 |
| v1.12.0 | 2026-08-31 | **개선 결과를 표에서 카드 그리드로** — 지표 하나 = 카드 하나(auto-fit 224px). 4단 구성(라벨 → 개선값 → 감소율 배지 → 전후 미니바). 미니바 폭은 이전=100% 기준이라 숫자를 안 읽어도 줄어든 정도가 보인다 |
| v1.11.0 | 2026-08-31 | **개선 결과 패널(.perf) 신설** — 성능 수치를 카드 본문에 흩지 말고 핵심 정리 바로 아래에 before → after 로 모은다(3~6행, 무번호 ⚡). 항목 이름은 사용자 관점으로. 성능 개선이 없는 날은 통째 삭제 |
| v1.10.0 | 2026-08-31 | **독자를 의사결정자로 못박고 한도 4개 신설** — ①카드당 불릿 4개 ②불릿 40자·한 문장 한 개념 ③강조는 수치에만·불릿당 1개 ④카드 3~4장(대상 단위). 개발 내부 얘기 금지 대조표 + 판정 한 줄. 실측 근거: 카드당 불릿 **8.7개** · 길이 중앙 **44자**(최대 105) · `<b>` 불릿당 **1.9개**(다 굵어 위계 없음) · before/after 도식은 v1.7.0 이후 한 번도 안 쓰임 |
| v1.9.0 | 2026-08-24 | **작업 제목을 깔끔한 한 문장 평서문으로** — `~해서 ~했던 문제` 이중절과 `~던 문제` 접미 금지, 종결 `~다` 통일. 금지 3종·예시 대조표·FEAT 도 현상으로 쓴다는 단서. 사유: 제목이 원인과 현상을 함께 담아 길어지고 본문 원인 칸과 중복됐다 |
| v1.8.2 | 2026-07-27 | **커밋 = 검증 완료 연동** — 커밋 지시는 사용자 수동 검증이 끝났다는 뜻이므로 그 커밋의 작업 카드는 `✓ 사용자 확인` 배지 유지 + 검증 패널의 사용자 수동 항목을 완료(✓)로 기재. "미검증/대기" 표기 금지 |
| v1.8.1 | 2026-07-10 | **브랜치명 재스캔 규칙** — 생성/갱신 때마다 헤더 "브랜치" 메타를 현재 브랜치로 다시 스캔해 갱신. 초안이 임시 브랜치로 작성되는 게 기본이라 rename 후 교체 |
| v1.8.0 | 2026-07-10 | **카드 = 태스크(대상) 단위 병합 규칙** — 카드를 커밋이 아니라 대상 단위로. 같은 날 같은 대상의 보충 구현·후속 버그픽스는 그 대상의 큰 카드에 짧은 불릿으로 통합. 다른 대상은 여전히 별도 카드 |
| v1.7.1 | 2026-07-09 | **드라이브 업로드에서 HTML 제외** — `$UPLOAD_HTML` 기본값 `$false`(PDF만). dedup 이 `.pdf`·`.html` 모두 지우므로 이전 HTML 잔존 없음 |
| v1.7.0 | 2026-07-06 | **디자인 리파인 + before/after 도식 컴포넌트** — ①팔레트를 슬레이트 중성색 + 딥 틸 accent 로 리파인 ②뷰어 토글 대응 `:root[data-theme]` 오버라이드 추가(media query 양방향 승리) ③`.figure`/`.ba` 도식 컴포넌트 신설(인라인 SVG `.d-*` 토큰) |
| v1.6.0 | 2026-07-03 | **PDF 클릭 목차 (Drive 뷰어 대응)** — `@media print` 에서 목차를 숨기지 않고 본문 상단 정적 블록으로 노출. 앵커가 print-to-pdf 에서 PDF 내부 링크로 변환돼 Drive PDF 뷰어에서 목차 클릭 이동이 작동. 점프바는 계속 숨김 |
| v1.5.1 | 2026-07-03 | **파일명에 작성자 추가 + dedup 확장** — `{YYMMDD}-일일보고서-{작성자} 연구원`, 드라이브 dedup 을 YYMMDD 접두 전체 매칭으로 확장(이름 변형 무관) |
| v1.5.0 | 2026-07-03 | **파일명 순서 변경 + 드라이브 업데이트 정책** — `일일보고서-YYYY-MM-DD` → `{YYMMDD}-일일보고서`(날짜 접두, 목록 날짜순 정렬). 같은 날짜 재발행 시 기존 것 삭제 후 재업로드(날짜당 1건) |
| v1.4.1 | 2026-07-02 | **목차 클릭 이동 수정** — 아티팩트는 `allow-scripts` 샌드박스 iframe(불투명 origin)이라 네이티브 해시 앵커 이동이 navigation 으로 차단된다. 인페이지 해시 링크 클릭 핸들러(`scrollIntoView` + `history.replaceState`)로 우회 |
| v1.4.0 | 2026-07-02 | **TOC 반응형 3단** — 1024~1259px 중간 폭에서 레일을 좌측 고정 + 본문 우측 시프트로 **플로팅 유지**(기존엔 <1260px 에서 통째 숨김 → 아티팩트 뷰어 패널에서 목차가 점프바로만 보이던 회귀). 점프바 폴백은 <1024px 로 하향 |
| v1.3.0 | 2026-06-29 | **핵심 정리 섹션 신설** — 맨 위(집계 칩 아래)에 그날 작업을 종류별로 병합한 한눈 요약 패널(그룹당 한 줄 + 카드번호 점프). TOC·점프바 최상단 `★ 핵심 정리` 항목 |
| v1.2.2 | 2026-06-29 | "커밋해" 트리거 규칙 추가 — 커밋 지시 시 그날 보고서 자동 생성/갱신(동일 file_path 재사용) |
| v1.2.1 | 2026-06-26 | 줄바꿈(워드랩) 규칙 추가 — 한글 `word-break: keep-all`(어절 단위, 제목 글자 잘림 방지) + `overflow-wrap: break-word` |
| v1.2.0 | 2026-06-26 | 본문 표기 규칙 — 원인·개선 결과를 줄글 → **개조식(불릿)** 강제. `.val` 에 `<ul>` + 지원 CSS |
| v1.1.0 | 2026-06-26 | 전 패키지 공용화 — 특정 패키지 편향 제거(제목/그룹 예시·게이트·검증 연계 일반화) |
| v1.0.0 | 2026-06-26 | 초기 — 제목(현상)+원인→개선결과 카드, 좌측 TOC·좁은화면 점프바, 두 자리 넘버링, 영향·부작용 패널, 검증, 다크/인쇄, 색 토큰 고정, 전체 작업수 칩, 재사용 프레임 분리 |
